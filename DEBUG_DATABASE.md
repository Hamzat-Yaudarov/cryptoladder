# 🔧 Диагностика ошибки DATABASE_URL

## Ошибка: `getaddrinfo ENOTFOUND`

Это означает что приложение не может найти/подключиться к серверу базы данных.

---

## ✅ Шаг 1: Проверьте переменную окружения в Railway

1. Откройте Railway dashboard
2. Выберите свой проект
3. Перейдите на вкладку **Variables**
4. Найдите `DATABASE_URL`
5. Скопируйте значение полностью

**Правильный формат:**
```
postgresql://neondb_owner:npg_9E0jKXaBbpQm@ep-long-dream-ageb5l8j-pooler.c-2.eu-central-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
```

**Проверка:**
- ✅ Начинается с `postgresql://`
- ✅ Содержит хост (между @ и /)
- ✅ Содержит параметры `?sslmode=require`
- ✅ Нет пробелов в начале/конце
- ✅ Нет переносов строк

---

## ❌ Распространённые ошибки

### 1. Строка скопирована неправильно
**Проблема**: Вместо полной строки скопировалась только часть

**Решение**:
```bash
# Правильная строка для Neon:
postgresql://neondb_owner:npg_9E0jKXaBbpQm@ep-long-dream-ageb5l8j-pooler.c-2.eu-central-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
```

### 2. Пробелы или переносы строк
**Проблема**: Лишние символы в начале/конце

**Решение**:
```
❌ Неправильно:
  postgresql://...

✅ Правильно:
postgresql://...
```

### 3. Неправильный хост
**Проблема**: Хост Neon неверный или база удалена

**Решение**:
- Проверьте, что БД всё ещё существует на https://console.neon.tech
- Скопируйте connection string прямо из Neon

### 4. SSL ошибка
**Проблема**: Railway использует другую версию SSL

**Решение**: Уже добавлена в код, но если проблема:
```
Добавьте в DATABASE_URL:
?sslmode=require
```

---

## 🔍 Шаг 2: Проверьте статус Neon БД

1. Откройте https://console.neon.tech
2. Выберите ваш проект
3. Проверьте **Connection String** в разделе "Connection details"
4. Убедитесь что проект **Active** (не suspended)

**Если проект suspended:**
- Используется free tier с ограничениями
- Может быть отключен после неактивности
- Нужно активировать заново

---

## 📝 Шаг 3: Обновите переменную в Railway

1. Railway dashboard → Variables
2. Удалите старую `DATABASE_URL`
3. Добавьте новую со свежей копией из Neon:

```
postgresql://neondb_owner:npg_9E0jKXaBbpQm@ep-long-dream-ageb5l8j-pooler.c-2.eu-central-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
```

4. Нажмите **Save**
5. Railway автоматически перезагрузит контейнер

---

## ���� Шаг 4: Проверьте логи

После обновления переменной:

1. Railway dashboard → Deploy logs
2. Смотрите последний деплой
3. Ищите сообщение:
   - ✅ `✅ Database connected` — БД работает!
   - ❌ `⚠️  Database unavailable` — всё ещё проблема

---

## 🆘 Если всё ещё не работает

### Вариант A: Переподключитесь к Railway
```bash
railway login
railway init
railway up
```

### Вариант B: Создайте новую БД в Neon
1. https://console.neon.tech
2. Create project (ново)
3. Скопируйте connection string
4. Обновите в Railway

### Вариант C: Используйте Supabase вместо Neon
Если Neon всё ещё не работает, можно подключить [Connect to Supabase](#open-mcp-popover) как альтернативу.

---

## 📋 Checklist перед рестартом

- [ ] DATABASE_URL скопирована полностью
- [ ] Нет пробелов/переносов в начале/конце
- [ ] Строка начинается с `postgresql://`
- [ ] Содержит `?sslmode=require`
- [ ] Neon проект активен (не suspended)
- [ ] Railway переменная обновлена
- [ ] Контейнер перезагрузился

После этого сервер должен запуститься! 🎉

---

## 💡 Быстрая проверка

Просто скопируйте и используйте эту строку подключения (она уже в коде):

```
postgresql://neondb_owner:npg_9E0jKXaBbpQm@ep-long-dream-ageb5l8j-pooler.c-2.eu-central-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
```

Если Railway переменная пуста — используйте эту!
